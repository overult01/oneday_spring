<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd">
	
	<!-- Root Context: defines shared resources visible to all other web components -->

<!-- DB접속하기 위한 정보 설정  -->
<!-- for mysql -->
<!-- dataSource: DB관련 정보  -->
	<bean id = "dataSource"
		class="org.apache.commons.dbcp2.BasicDataSource"
		destroy-method="close">
  	<property name="driverClassName"
  	 value="com.mysql.cj.jdbc.Driver" />
 	 <property name="url"
 	 value="jdbc:mysql://localhost:3306/oneday?serverTimezone=UTC&amp;useSSL=false&amp;useUnicode=true&amp;characterEncoding=UTF-8" />
 	 <property name="username" value="emp" />
 	 <property name="password" value="12345678" />
	</bean>
	
<!-- 	sqlSessionFactory: DB와 연결을 맺고 끊어질 때까지의 라이프 사이클을 관리하는 sqlSession 객체를 만든다. -->
<!-- 프로퍼티는 두개. 데이터 소스를 설정하는 곳, 매퍼 파일의 위치 지정 xml  -->
	<bean id="sqlSessionFactory"
	class="org.mybatis.spring.SqlSessionFactoryBean">
	<property name="dataSource" ref="dataSource" />
	<property name="mapperLocations"
	value="classpath:/sqlmap/**/*_SQL.xml" /> <!--  classpath:/sqlmap/**/*_SQL.xml -> /src/main/resources/sqlmap/ 로 시작하고, _SQL.xml로 끝나는 파일들을 매퍼파일로 쓰겠다고 스프링에게 전달  -->
	</bean>
 
<!-- sqlSessionTemplate: sqlSessionFactory 객체를 생성자로 받는다. 따라서 이 객체는 sqlSession객체가 가지고 있는 DB접속정보, 매퍼파일의 위치를 알 수 있게 된다. -->
 	<bean id="sqlSessionTemplate"
	class="org.mybatis.spring.SqlSessionTemplate">
	<constructor-arg index="0" ref="sqlSessionFactory" />
 	</bean> 	
</beans>
